# similar to solver_history.prm, but check the stokes residual postprocessor
# instead of using the signal directly.

include $ASPECT_SOURCE_DIR/tests/pressure_compatibility.prm

set Number of cheap Stokes solver steps    = 0
